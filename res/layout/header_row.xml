<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="#336699" >

    <Button
        android:id="@+id/btn_filebrowser_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_filebrowser_back" 
        android:layout_alignParentLeft="true"
        android:drawableLeft="@drawable/icon_back"
        android:drawablePadding="10dp"
        android:background="@android:drawable/btn_default" 
        android:layout_margin="5dp" />
    
     <ImageButton
        android:id="@+id/btn_refresh_filelist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:src="@drawable/icon_refresh"
        android:background="@android:drawable/btn_default" 
        android:layout_margin="5dp" />
     
    <TextView
        android:id="@+id/txv_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/btn_filebrowser_back"
        android:padding="10dp"
        android:text="@string/lbl_basedir"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold" 
        android:layout_margin="5dp" />
    
   
</RelativeLayout>